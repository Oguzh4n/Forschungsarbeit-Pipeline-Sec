# Spring Boot application configuration
spring.application.name=pipeline-sec-demo
server.port=8080

# ============================================================
# WARNING: The following credentials were accidentally committed
# as part of this demo PR. They are non-functional example values
# (AWS's own documentation placeholder keys) used to demonstrate
# that TruffleHog detects credential patterns in PR diffs and
# blocks the merge before the secrets reach the main branch.
#
# In a real incident a developer would commit actual working keys
# here — TruffleHog's job is to catch exactly this pattern.
# ============================================================
cloud.aws.credentials.access-key=AKIAIOSFODNN7EXAMPLE
cloud.aws.credentials.secret-key=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
cloud.aws.region.static=us-east-1
