# Spring Boot application configuration
spring.application.name=pipeline-sec-demo
server.port=8080

# ============================================================
# WARNING: The following credentials were accidentally committed
# as part of this demo PR. They are non-functional, format-valid
# placeholder values used to demonstrate that TruffleHog detects
# credential patterns in PR diffs and blocks the merge before the
# secrets reach the main branch.
#
# Note: AKIAIOSFODNN7EXAMPLE (the AWS documentation example key)
# is in TruffleHog's internal allowlist and is not detected.
# The key below uses the same AKIA[A-Z0-9]{16} format but is not
# allowlisted, so TruffleHog will report it as an unverified match.
#
# In a real incident a developer would commit actual working keys
# here — TruffleHog's job is to catch exactly this pattern.
# ============================================================
cloud.aws.credentials.access-key=AKIAFAKEDEMOKEY00001
cloud.aws.credentials.secret-key=FaKeDeMoSeCrEtKeY/NotReal+ForResearchOnly
cloud.aws.region.static=us-east-1
